# Front local apontando para a API real (recomendado)
# Deixe VITE_API_URL vazio — o Vite faz proxy de /api e /health
# VITE_API_URL=

VITE_DEV_PROXY_TARGET=https://app.muratorimkt.com.br

# Só use API local se estiver desenvolvendo backend com Docker/Postgres local:
# VITE_DEV_PROXY_TARGET=http://127.0.0.1:3340
